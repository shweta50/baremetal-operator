FROM alpine:3.11

MAINTAINER support@platform9.com

RUN apk add curl 

COPY ./bin/manager /
COPY addon_templates /addon_templates
COPY etc/addon /etc/addon
RUN chmod +x /manager
ENTRYPOINT ["/manager"]
#CMD sh -c "sleep 1000000"
